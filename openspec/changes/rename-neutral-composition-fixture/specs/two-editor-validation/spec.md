## REMOVED Requirements

### Requirement: Minimal validation components
**Reason**: Capability renamed to `neutral-composition-fixture` — the package's
role is now the shared neutral composition every adapter mounts, not a
single-adapter validation; "two-editor" ambiguously read as adapter count.
**Migration**: See `neutral-composition-fixture`'s "Minimal validation
components" requirement — same behavior under the new capability name.

### Requirement: Two isolated editors coexist
**Reason**: Capability renamed to `neutral-composition-fixture`.
**Migration**: See `neutral-composition-fixture`'s "Two isolated editors
coexist" requirement — unchanged behavior.

### Requirement: Template change preserves business events
**Reason**: Capability renamed to `neutral-composition-fixture`.
**Migration**: See `neutral-composition-fixture`'s "Template change preserves
business events" requirement — unchanged behavior.

### Requirement: Scoped disposal cancels only owned work
**Reason**: Capability renamed to `neutral-composition-fixture`.
**Migration**: See `neutral-composition-fixture`'s "Scoped disposal cancels
only owned work" requirement — unchanged behavior.

### Requirement: End-to-end scenario in a browser-like environment
**Reason**: Capability renamed to `neutral-composition-fixture`.
**Migration**: See `neutral-composition-fixture`'s "End-to-end scenario in a
browser-like environment" requirement — unchanged behavior.

### Requirement: Interaction routed through the neutral port
**Reason**: Capability renamed to `neutral-composition-fixture`.
**Migration**: See `neutral-composition-fixture`'s "Interaction routed through
the neutral port" requirement — unchanged behavior.

### Requirement: Renderer-agnostic composition and shared test-drive surface
**Reason**: Capability renamed to `neutral-composition-fixture`.
**Migration**: See `neutral-composition-fixture`'s "Renderer-agnostic
composition and shared test-drive surface" requirement — unchanged behavior.
